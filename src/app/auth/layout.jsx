/**
 * Layout pour les pages /auth/* — sans Sidebar, plein écran.
 * Les pages auth ont leur propre layout indépendant du layout racine.
 */
export default function AuthLayout({ children }: { children: any }) {
  return <>{children}</>
}
